#ifndef GUARD_extract_fails_h
#define GUARD_extract_fails_h

// Extract_fails header
#include <vector>
#include <list>
#include "Student_info.h"
#include "Group.h"
Group extract_fails(Group&);
bool fgrade(const Student_info&);

#endif